{
  "name": "mimi",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "sync-vars": "bun scripts/sync-dev-vars.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.54.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.11.3",
    "jose": "^6.1.3"
  }
}
